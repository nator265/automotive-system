return (
    <div className="bg-gray-50 py-20 px-10 z-3 relative">
=======
const ReviewsAndStats = () => {
  return (
    <div
      className="relative py-20 px-10 z-3 bg-fixed bg-center bg-cover"
      style={{ backgroundImage: "url('/cars/rav4_2.jpg')" }}
    >
      {/* Overlay to dim background for better text readability */}
      <div className="absolute inset-0 bg-black bg-opacity-40 z-0"></div>

      {/* Content Container */}
      <div className="relative max-w-7xl mx-auto z-10">
>>>>>>> REPLACE

<<<<<<< SEARCH
      {/* Statistics */}
      <div className="max-w-7xl mx-auto mb-16">
        <h2 className="text-3xl font-bold text-center mb-10">
          Our Achievements
        </h2>
        <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-6 gap-8 text-center">
          {stats.map((stat) => (
            <div
              key={stat.label}
              className="p-6 bg-white rounded-lg shadow hover:shadow-lg transition"
            >
              <p className="text-4xl font-bold text-blue-600">{stat.value}</p>
              <p className="text-gray-600 mt-2">{stat.label}</p>
            </div>
          ))}
        </div>
      </div>

      {/* Reviews */}
      <div className="max-w-5xl mx-auto">
        <h2 className="text-3xl font-bold text-center mb-10">
          What Our Customers Say
        </h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8">
          {reviews.map((review, index) => (
            <div
              key={index}
              className="p-6 bg-white rounded-lg shadow hover:shadow-lg transition"
            >
              <p className="text-gray-700 mb-4">"{review.comment}"</p>
              <div className="flex items-center justify-between">
                <p className="font-semibold">{review.name}</p>
                <p className="text-yellow-500">
                  {"★".repeat(review.rating)}
                  {"☆".repeat(5 - review.rating)}
                </p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
};
=======
      {/* Statistics */}
      <div className="mb-20">
        <h2 className="text-4xl font-semibold text-center text-white mb-12 tracking-wide">
          Our Achievements
        </h2>
        <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-6 gap-10 text-center">
          {stats.map((stat) => (
            <div
              key={stat.label}
              className="p-6 bg-white bg-opacity-20 rounded-xl backdrop-blur-md transition hover:bg-opacity-30 cursor-default"
            >
              <p className="text-5xl font-extrabold text-blue-400">{stat.value}</p>
              <p className="text-white mt-3 text-lg font-light">{stat.label}</p>
            </div>
          ))}
        </div>
      </div>

      {/* Reviews */}
      <div className="max-w-5xl mx-auto">
        <h2 className="text-4xl font-semibold text-center text-white mb-12 tracking-wide">
          What Our Customers Say
        </h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-12">
          {reviews.map((review, index) => (
            <div
              key={index}
              className="p-8 bg-white bg-opacity-20 rounded-xl backdrop-blur-md transition hover:bg-opacity-30 cursor-default shadow-none"
            >
              <p className="text-white mb-5 italic text-base leading-relaxed">
                "{review.comment}"
              </p>
              <div className="flex items-center justify-between">
                <p className="font-semibold text-white text-lg">{review.name}</p>
                <p className="text-yellow-400 text-xl select-none">
                  {"★".repeat(review.rating)}
                  {"☆".repeat(5 - review.rating)}
                </p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
};
>>>>>>> REPLACE
